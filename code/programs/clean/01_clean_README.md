# 01_clean.R

**What it does:** Loads the raw data, renames columns, filters to relevant rows, handles missing values, and saves a clean version.

**Inputs:** `data/clean/[raw file]`

**Outputs:** `data/processed/data_clean.rds`
